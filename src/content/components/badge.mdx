---
title: Badge
description: A span with utilities. Included so you can stop wondering whether the library has one.
order: 40
plainCss: true
features: []
---

import ComponentPreview from '../../components/ComponentPreview.astro';

<ComponentPreview demo="badge/variants" />

## How it works

- One `<span>`, four utility sets. Use `<a>` instead if it links somewhere — same classes work.
- If the badge conveys status ("Failed", "Live"), don't rely on color alone — the text is
  the accessible part.
